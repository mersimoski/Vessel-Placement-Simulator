@namespace BlazorApp.Features.VesselPlacement.Components

<div class="alert alert-danger" role="alert">
    @Message
    @if (OnRetry.HasDelegate)
    {
        <button class="btn btn-primary mt-2" @onclick="HandleRetryClick">@RetryButtonText</button>
    }
</div>
